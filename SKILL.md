---
name: arabic-web-slides
description: إنشاء عروض تقديمية ويب HTML غنية بالحركات والتأثيرات من الصفر أو بتحويل ملفات PowerPoint. استخدم هذه المهارة عندما يريد المستخدم بناء عرض تقديمي، تحويل PPT/PPTX إلى ويب، أو إنشاء شرائح لمحاضرة أو عرض. تدعم اللغة العربية بالكامل مع تنسيق RTL.
---

# مهارة العروض التقديمية الأمامية

إنشاء عروض تقديمية HTML غنية بالحركات تعمل بالكامل في المتصفح بدون أي تبعيات. تساعد هذه المهارة غير المصممين على اكتشاف جمالياتهم المفضلة من خلال الاستكشاف البصري ("اعرض، لا تشرح")، ثم تُنتج عروض شرائح بجودة إنتاجية.

## الفلسفة الأساسية

1. **بدون تبعيات** — ملفات HTML فردية مع CSS/JS مضمّن. لا npm، لا أدوات بناء.
2. **اعرض، لا تشرح** — الناس لا يعرفون ما يريدون حتى يروه. قم بإنشاء معاينات بصرية، وليس خيارات مجردة.
3. **تصميم مميز** — تجنب جماليات "AI slop" العامة. كل عرض تقديمي يجب أن يبدو مصنوعاً يدوياً.
4. **جودة إنتاجية** — الكود يجب أن يكون معلقاً جيداً، متاحاً، وفعالاً.
5. **دعم العربية و RTL** — جميع العروض تدعم اللغة العربية والاتجاه من اليمين لليسار بشكل كامل.

---

## المرحلة 0: كشف الوضع

أولاً، حدد ما يريده المستخدم:

**الوضع أ: عرض تقديمي جديد**
- المستخدم يريد إنشاء شرائح من الصفر
- انتقل إلى المرحلة 1 (اكتشاف المحتوى)

**الوضع ب: تحويل PPT**
- المستخدم لديه ملف PowerPoint (.ppt, .pptx) للتحويل
- انتقل إلى المرحلة 4 (استخراج PPT)

**الوضع ج: تحسين عرض موجود**
- المستخدم لديه عرض HTML ويريد تحسينه
- اقرأ الملف الموجود، افهم البنية، ثم حسّنه

---

## المرحلة 1: اكتشاف المحتوى (عروض جديدة)

قبل التصميم، افهم المحتوى. اسأل عبر AskUserQuestion:

### الخطوة 1.1: سياق العرض التقديمي

**السؤال 1: الغرض**
- العنوان: "الغرض"
- السؤال: "ما هو الغرض من هذا العرض التقديمي؟"
- الخيارات:
  - "عرض تقديمي للمستثمرين" — بيع فكرة، منتج، أو شركة للمستثمرين/العملاء
  - "تعليم/شرح" — شرح مفاهيم، أدلة إرشادية، محتوى تعليمي
  - "محاضرة في مؤتمر" — التحدث في حدث، محاضرة تقنية، كلمة افتتاحية
  - "عرض داخلي" — تحديثات الفريق، اجتماعات استراتيجية، تحديثات الشركة

**السؤال 2: عدد الشرائح**
- العنوان: "الطول"
- السؤال: "كم عدد الشرائح تقريباً؟"
- الخيارات:
  - "قصير (5-10)" — عرض سريع، محاضرة خاطفة
  - "متوسط (10-20)" — عرض تقديمي قياسي
  - "طويل (20+)" — تعمق، محاضرة شاملة

**السؤال 3: المحتوى**
- العنوان: "المحتوى"
- السؤال: "هل المحتوى جاهز لديك، أم تحتاج مساعدة في هيكلته؟"
- الخيارات:
  - "لدي كل المحتوى جاهز" — أحتاج فقط تصميم العرض
  - "لدي ملاحظات أولية" — أحتاج مساعدة في تنظيمها إلى شرائح
  - "لدي موضوع فقط" — أحتاج مساعدة في إنشاء المخطط الكامل

إذا كان لدى المستخدم محتوى، اطلب منه مشاركته (نص، نقاط، صور، إلخ).

---

## المرحلة 2: اكتشاف الأسلوب (الاستكشاف البصري)

> ⛔ **تحذير إلزامي - لا يمكن تجاوز هذه المرحلة**
> 
> **يجب على الوكيل (Agent) الالتزام بالقواعد التالية بدون استثناء:**
> 
> 1. **إنشاء 3 معاينات أسلوب إلزامي** — يجب دائماً إنشاء 3 ملفات HTML للمعاينة قبل إنشاء العرض النهائي. لا يُسمح بتجاوز هذه الخطوة أبداً.
> 
> 2. **لا تختار نيابة عن المستخدم** — يجب على المستخدم اختيار الأسلوب بنفسه. لا يجوز للوكيل اتخاذ قرار التصميم بناءً على افتراضات حول طبيعة المحتوى أو الموضوع.
> 
> 3. **الاستثناء الوحيد** — يُسمح للوكيل باختيار الأسلوب فقط إذا طلب المستخدم صراحةً أن يختار الوكيل نيابة عنه (مثل: "اختر لي"، "قرر أنت"، "أي أسلوب تراه مناسباً").
> 
> 4. **في حالة الشك** — اسأل المستخدم دائماً. إذا لم تكن متأكداً مما يريده المستخدم، اعرض الخيارات واطلب منه الاختيار.
> 
> **⚠️ انتهاك هذه القواعد يُعتبر خطأ في تنفيذ المهارة.**

**مهم: هذه هي مرحلة "اعرض، لا تشرح".**

معظم الناس لا يستطيعون التعبير عن تفضيلات التصميم بالكلمات. بدلاً من السؤال "هل تريد بسيط أم جريء؟"، ننشئ معاينات صغيرة وندعهم يتفاعلون.

### الخطوة 2.1: اختيار المزاج

**السؤال 1: الشعور**
- العنوان: "الأجواء"
- السؤال: "ما الشعور الذي يجب أن يشعر به الجمهور عند مشاهدة شرائحك؟"
- الخيارات:
  - "معجب/واثق" — احترافي، جدير بالثقة، هذا الفريق يعرف ما يفعله
  - "متحمس/نشيط" — مبتكر، جريء، هذا هو المستقبل
  - "هادئ/مركّز" — واضح، مدروس، سهل المتابعة
  - "ملهم/مؤثر" — عاطفي، سرد قصصي، لا يُنسى
- multiSelect: true (يمكن اختيار حتى 2)

### الخطوة 2.2: إنشاء معاينات الأسلوب

بناءً على اختيار المزاج، أنشئ **3 معاينات أسلوب مختلفة** كملفات HTML صغيرة في مجلد مؤقت. كل معاينة يجب أن تكون شريحة عنوان واحدة تُظهر:

- الخطوط (اختيارات الخط، تسلسل العنوان/النص)
- لوحة الألوان (الخلفية، اللون المميز، ألوان النص)
- أسلوب الحركة (كيف تدخل العناصر)
- الشعور الجمالي العام

**أساليب المعاينة للنظر فيها (اختر 3 بناءً على المزاج):**

| المزاج | خيارات الأسلوب |
|------|-----------------|
| معجب/واثق | "أناقة مؤسسية"، "تنفيذي داكن"، "بساطة نظيفة" |
| متحمس/نشيط | "سايبر نيون"، "تدرجات جريئة"، "حركة حيوية" |
| هادئ/مركز | "ورق وحبر"، "ألوان هادئة"، "بساطة سويسرية" |
| ملهم/مؤثر | "سينمائي داكن"، "تحرير دافئ"، "جو غامض" |

**مهم: لا تستخدم هذه الأنماط العامة أبداً:**
- تدرجات بنفسجية على خلفيات بيضاء
- خطوط Inter، Roboto، أو خطوط النظام
- ألوان أزرق أساسية قياسية
- تخطيطات hero متوقعة

**بدلاً من ذلك، استخدم خيارات مميزة:**
- تزاوج خطوط فريد (Tajawal، Cairo، Noto Kufi Arabic، Amiri، إلخ.)
- سمات ألوان متناسقة ذات شخصية
- خلفيات جوية (تدرجات، أنماط دقيقة، عمق)
- لحظات حركة مميزة

### الخطوة 2.3: عرض المعاينات

أنشئ المعاينات في: `.claude-design/slide-previews/`

```
.claude-design/slide-previews/
├── style-a.html   # خيار الأسلوب الأول
├── style-b.html   # خيار الأسلوب الثاني
├── style-c.html   # خيار الأسلوب الثالث
└── assets/        # أي أصول مشتركة
```

كل ملف معاينة يجب أن يكون:
- مكتفٍ ذاتياً (CSS/JS مضمّن)
- "شريحة عنوان" واحدة تُظهر الجمالية
- متحرك لإظهار أسلوب الحركة
- ~50-100 سطر، وليس عرضاً كاملاً

قدّم للمستخدم:
```
لقد أنشأت 3 معاينات أسلوب للمقارنة:

**الأسلوب أ: [الاسم]** — [جملة وصف واحدة]
**الأسلوب ب: [الاسم]** — [جملة وصف واحدة]
**الأسلوب ج: [الاسم]** — [جملة وصف واحدة]

افتح كل ملف لرؤيتها قيد التشغيل:
- .claude-design/slide-previews/style-a.html
- .claude-design/slide-previews/style-b.html
- .claude-design/slide-previews/style-c.html

ألقِ نظرة وأخبرني:
1. أي أسلوب يتردد صداه أكثر؟
2. ما الذي يعجبك فيه؟
3. هل تريد تغيير أي شيء؟
```

ثم استخدم AskUserQuestion:

**السؤال: اختر أسلوبك**
- العنوان: "الأسلوب"
- السؤال: "أي معاينة أسلوب تفضل؟"
- الخيارات:
  - "الأسلوب أ: [الاسم]" — [وصف مختصر]
  - "الأسلوب ب: [الاسم]" — [وصف مختصر]
  - "الأسلوب ج: [الاسم]" — [وصف مختصر]
  - "مزج العناصر" — دمج جوانب من أساليب مختلفة

إذا اختار "مزج العناصر"، اسأل عن التفاصيل.

---

## المرحلة 3: إنشاء العرض التقديمي

الآن أنشئ العرض الكامل بناءً على:
- المحتوى من المرحلة 1
- الأسلوب من المرحلة 2

### بنية الملفات

لعروض فردية:
```
presentation.html    # عرض مكتفٍ ذاتياً
assets/              # الصور، إن وجدت
```

لمشاريع بعروض متعددة:
```
[اسم-العرض].html
[اسم-العرض]-assets/
```

### بنية HTML

اتبع هذه البنية لجميع العروض:

```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عنوان العرض التقديمي</title>

    <!-- الخطوط (استخدم خطوط Google العربية) -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800&family=Cairo:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* ===========================================
           خصائص CSS المخصصة (السمة)
           سهلة التعديل: غيّر هذه لتغيير الشكل الكامل
           =========================================== */
        :root {
            /* الاتجاه */
            --direction: rtl;

            /* الألوان */
            --bg-primary: #0a0f1c;
            --bg-secondary: #111827;
            --text-primary: #ffffff;
            --text-secondary: #9ca3af;
            --accent: #00ffcc;
            --accent-glow: rgba(0, 255, 204, 0.3);

            /* الخطوط */
            --font-display: 'Tajawal', sans-serif;
            --font-body: 'Cairo', sans-serif;

            /* التباعد */
            --slide-padding: clamp(2rem, 5vw, 4rem);

            /* الحركة */
            --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
            --duration-normal: 0.6s;
        }

        /* ===========================================
           الأنماط الأساسية
           =========================================== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            scroll-snap-type: y mandatory;
            direction: rtl;
        }

        body {
            font-family: var(--font-body);
            background: var(--bg-primary);
            color: var(--text-primary);
            overflow-x: hidden;
            text-align: right;
            line-height: 1.8;
        }

        /* ===========================================
           حاوية الشريحة
           كل قسم هو شريحة واحدة
           =========================================== */
        .slide {
            min-height: 100vh;
            padding: var(--slide-padding);
            scroll-snap-align: start;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        /* ===========================================
           الحركات
           تُفعّل عبر صف .visible (يُضاف بواسطة JS عند التمرير)
           =========================================== */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity var(--duration-normal) var(--ease-out-expo),
                        transform var(--duration-normal) var(--ease-out-expo);
        }

        .slide.visible .reveal {
            opacity: 1;
            transform: translateY(0);
        }

        /* ظهور من اليمين (للـ RTL) */
        .reveal-right {
            opacity: 0;
            transform: translateX(-50px);
            transition: opacity 0.6s, transform 0.6s var(--ease-out-expo);
        }

        .slide.visible .reveal-right {
            opacity: 1;
            transform: translateX(0);
        }

        /* ظهور من اليسار (للـ RTL) */
        .reveal-left {
            opacity: 0;
            transform: translateX(50px);
            transition: opacity 0.6s, transform 0.6s var(--ease-out-expo);
        }

        .slide.visible .reveal-left {
            opacity: 1;
            transform: translateX(0);
        }

        /* تأخير متتابع للعناصر الفرعية */
        .reveal:nth-child(1) { transition-delay: 0.1s; }
        .reveal:nth-child(2) { transition-delay: 0.2s; }
        .reveal:nth-child(3) { transition-delay: 0.3s; }
        .reveal:nth-child(4) { transition-delay: 0.4s; }

        /* ===========================================
           نقاط التنقل (على اليسار للـ RTL)
           =========================================== */
        .nav-dots {
            position: fixed;
            left: 2rem;
            top: 50%;
            transform: translateY(-50%);
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
            z-index: 100;
        }

        .nav-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: var(--text-secondary);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .nav-dot.active {
            background: var(--accent);
            box-shadow: 0 0 10px var(--accent-glow);
        }

        /* ===========================================
           شريط التقدم
           =========================================== */
        .progress-bar {
            position: fixed;
            top: 0;
            right: 0;
            height: 3px;
            background: var(--accent);
            width: 0%;
            z-index: 101;
            transition: width 0.2s ease;
        }

        /* ===========================================
           دعم تقليل الحركة
           =========================================== */
        @media (prefers-reduced-motion: reduce) {
            .reveal,
            .reveal-right,
            .reveal-left {
                transition: opacity 0.3s ease;
                transform: none;
            }
        }

        /* ===========================================
           استجابة الجوال
           =========================================== */
        @media (max-width: 768px) {
            .nav-dots,
            .keyboard-hint {
                display: none;
            }

            .slide {
                padding: 1.5rem;
            }
        }

        /* ... أنماط إضافية ... */
    </style>
</head>
<body>
    <!-- شريط التقدم (اختياري) -->
    <div class="progress-bar"></div>

    <!-- نقاط التنقل (اختياري) -->
    <nav class="nav-dots" aria-label="تنقل الشرائح">
        <!-- يُنشأ بواسطة JS -->
    </nav>

    <!-- الشرائح -->
    <section class="slide title-slide">
        <h1 class="reveal">عنوان العرض التقديمي</h1>
        <p class="reveal">العنوان الفرعي أو اسم المقدم</p>
    </section>

    <section class="slide">
        <h2 class="reveal">عنوان الشريحة</h2>
        <p class="reveal">المحتوى...</p>
    </section>

    <!-- المزيد من الشرائح... -->

    <script>
        /* ===========================================
           متحكم العرض التقديمي
           يتعامل مع التنقل، الحركات، والتفاعلات
           =========================================== */

        class SlidePresentation {
            constructor() {
                this.slides = document.querySelectorAll('.slide');
                this.currentSlide = 0;
                this.navDots = document.querySelector('.nav-dots');
                this.progressBar = document.querySelector('.progress-bar');

                this.init();
            }

            init() {
                this.createNavDots();
                this.setupIntersectionObserver();
                this.setupKeyboardNav();
                this.setupTouchNav();
                this.updateProgress();
            }

            createNavDots() {
                this.slides.forEach((_, index) => {
                    const dot = document.createElement('button');
                    dot.className = 'nav-dot';
                    dot.setAttribute('aria-label', `الشريحة ${index + 1}`);
                    dot.addEventListener('click', () => this.goToSlide(index));
                    this.navDots.appendChild(dot);
                });
            }

            setupIntersectionObserver() {
                const observer = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.classList.add('visible');
                            const index = Array.from(this.slides).indexOf(entry.target);
                            this.setCurrentSlide(index);
                        }
                    });
                }, { threshold: 0.5 });

                this.slides.forEach(slide => observer.observe(slide));
            }

            setupKeyboardNav() {
                document.addEventListener('keydown', (e) => {
                    // السهم للأسفل أو السهم لليسار أو المسافة = التالي
                    if (e.key === 'ArrowDown' || e.key === 'ArrowLeft' || e.key === ' ') {
                        e.preventDefault();
                        this.nextSlide();
                    }
                    // السهم للأعلى أو السهم لليمين = السابق
                    if (e.key === 'ArrowUp' || e.key === 'ArrowRight') {
                        e.preventDefault();
                        this.prevSlide();
                    }
                });
            }

            setupTouchNav() {
                let touchStartY = 0;
                document.addEventListener('touchstart', (e) => {
                    touchStartY = e.touches[0].clientY;
                });

                document.addEventListener('touchend', (e) => {
                    const touchEndY = e.changedTouches[0].clientY;
                    const diff = touchStartY - touchEndY;

                    if (Math.abs(diff) > 50) {
                        if (diff > 0) {
                            this.nextSlide();
                        } else {
                            this.prevSlide();
                        }
                    }
                });
            }

            setCurrentSlide(index) {
                this.currentSlide = index;
                this.updateNavDots();
                this.updateProgress();
            }

            updateNavDots() {
                const dots = this.navDots.querySelectorAll('.nav-dot');
                dots.forEach((dot, index) => {
                    dot.classList.toggle('active', index === this.currentSlide);
                });
            }

            updateProgress() {
                const progress = ((this.currentSlide + 1) / this.slides.length) * 100;
                this.progressBar.style.width = `${progress}%`;
            }

            goToSlide(index) {
                this.slides[index].scrollIntoView({ behavior: 'smooth' });
            }

            nextSlide() {
                if (this.currentSlide < this.slides.length - 1) {
                    this.goToSlide(this.currentSlide + 1);
                }
            }

            prevSlide() {
                if (this.currentSlide > 0) {
                    this.goToSlide(this.currentSlide - 1);
                }
            }
        }

        // التهيئة
        new SlidePresentation();
    </script>
</body>
</html>
```

### ميزات JavaScript المطلوبة

يجب أن يتضمن كل عرض:

1. **صف SlidePresentation** — المتحكم الرئيسي
   - تنقل لوحة المفاتيح (الأسهم، المسافة)
   - دعم اللمس/السحب
   - تنقل عجلة الماوس
   - تحديثات شريط التقدم
   - نقاط التنقل

2. **Intersection Observer** — للحركات المُفعّلة بالتمرير
   - إضافة صف `.visible` عند دخول الشرائح إلى منطقة الرؤية
   - تشغيل حركات CSS بكفاءة

3. **تحسينات اختيارية** (بناءً على الأسلوب):
   - مؤشر مخصص مع ذيل
   - نظام جسيمات للخلفية (canvas)
   - تأثيرات parallax
   - ميلان 3D عند التمرير
   - أزرار مغناطيسية
   - حركات العداد

### متطلبات جودة الكود

**التعليقات:**
يجب أن يحتوي كل قسم على تعليقات واضحة توضح:
- ماذا يفعل
- لماذا موجود
- كيفية تعديله

```javascript
/* ===========================================
   مؤشر مخصص
   ينشئ مؤشراً مُنسّقاً يتبع الماوس بتأثير ذيل.
   - يستخدم lerp (الاستيفاء الخطي) لحركة سلسة
   - يكبر عند التمرير فوق العناصر التفاعلية
   =========================================== */
class CustomCursor {
    constructor() {
        // ...
    }
}
```

**إمكانية الوصول:**
- HTML دلالي (`<section>`، `<nav>`، `<main>`)
- تنقل لوحة المفاتيح يعمل
- تسميات ARIA حيث لزم
- دعم تقليل الحركة

```css
@media (prefers-reduced-motion: reduce) {
    .reveal {
        transition: opacity 0.3s ease;
        transform: none;
    }
}
```

**الاستجابة:**
- صديق للجوال (عمود واحد، تباعد معدّل)
- تعطيل التأثيرات الثقيلة على الجوال
- تفاعلات صديقة للسلم

```css
@media (max-width: 768px) {
    .nav-dots,
    .keyboard-hint {
        display: none;
    }
}
```

---

## المرحلة 4: تحويل PPT

عند تحويل ملفات PowerPoint:

### الخطوة 4.1: استخراج المحتوى

استخدم Python مع `python-pptx` لاستخراج:

```python
from pptx import Presentation
from pptx.util import Inches, Pt
import json
import os
import base64

def extract_pptx(file_path, output_dir):
    """
    استخراج كل المحتوى من ملف PowerPoint.
    يُرجع بنية JSON مع الشرائح، النص، والصور.
    """
    prs = Presentation(file_path)
    slides_data = []

    # إنشاء مجلد الأصول
    assets_dir = os.path.join(output_dir, 'assets')
    os.makedirs(assets_dir, exist_ok=True)

    for slide_num, slide in enumerate(prs.slides):
        slide_data = {
            'number': slide_num + 1,
            'title': '',
            'content': [],
            'images': [],
            'notes': ''
        }

        for shape in slide.shapes:
            # استخراج العنوان
            if shape.has_text_frame:
                if shape == slide.shapes.title:
                    slide_data['title'] = shape.text
                else:
                    slide_data['content'].append({
                        'type': 'text',
                        'content': shape.text
                    })

            # استخراج الصور
            if shape.shape_type == 13:  # صورة
                image = shape.image
                image_bytes = image.blob
                image_ext = image.ext
                image_name = f"slide{slide_num + 1}_img{len(slide_data['images']) + 1}.{image_ext}"
                image_path = os.path.join(assets_dir, image_name)

                with open(image_path, 'wb') as f:
                    f.write(image_bytes)

                slide_data['images'].append({
                    'path': f"assets/{image_name}",
                    'width': shape.width,
                    'height': shape.height
                })

        # استخراج الملاحظات
        if slide.has_notes_slide:
            notes_frame = slide.notes_slide.notes_text_frame
            slide_data['notes'] = notes_frame.text

        slides_data.append(slide_data)

    return slides_data
```

### الخطوة 4.2: تأكيد بنية المحتوى

قدّم المحتوى المستخرج للمستخدم:

```
لقد استخرجت التالي من PowerPoint الخاص بك:

**الشريحة 1: [العنوان]**
- [ملخص المحتوى]
- الصور: [العدد]

**الشريحة 2: [العنوان]**
- [ملخص المحتوى]
- الصور: [العدد]

...

تم حفظ جميع الصور في مجلد الأصول.

هل يبدو هذا صحيحاً؟ هل أتابع مع اختيار الأسلوب؟
```

### الخطوة 4.3: اختيار الأسلوب

تابع إلى المرحلة 2 (اكتشاف الأسلوب) مع المحتوى المستخرج في الاعتبار.

### الخطوة 4.4: إنشاء HTML

حوّل المحتوى المستخرج إلى الأسلوب المختار، مع الحفاظ على:
- كل محتوى النص
- جميع الصور (مرجعة من مجلد الأصول)
- ترتيب الشرائح
- أي ملاحظات للمتحدث (كتعليقات HTML أو ملف منفصل)

---

## المرحلة 5: التسليم

### الناتج النهائي

عند اكتمال العرض:

1. **تنظيف الملفات المؤقتة**
   - احذف `.claude-design/slide-previews/` إذا كانت موجودة

2. **افتح العرض التقديمي**
   - استخدم `open [اسم الملف].html` لتشغيله في المتصفح

3. **قدّم ملخصاً**
```
عرضك التقديمي جاهز!

📁 الملف: [اسم الملف].html
🎨 الأسلوب: [اسم الأسلوب]
📊 الشرائح: [العدد]

**التنقل:**
- مفاتيح الأسهم (← ← أو ↑ ↓) أو المسافة للتنقل
- التمرير/السحب يعمل أيضاً
- انقر على النقاط على اليسار للقفز إلى شريحة

**للتخصيص:**
- الألوان: ابحث عن متغيرات `:root` CSS في الأعلى
- الخطوط: غيّر رابط Google Fonts
- الحركات: عدّل توقيتات صف `.reveal`

هل تريد أي تعديلات؟
```

---

## مرجع الأسلوب: خريطة التأثير → الشعور

استخدم هذا الدليل لمطابقة الحركات مع المشاعر المقصودة:

### درامي / سينمائي
- ظهور بطيء (1-1.5 ثانية)
- انتقالات تدريج كبيرة (0.9 → 1)
- خلفيات داكنة مع تأثيرات إضاءة
- تمرير parallax
- صور كاملة العرض

### تقني / مستقبلي
- تأثيرات توهج نيون (box-shadow بلون التمييز)
- أنظمة جسيمات (خلفية canvas)
- أنماط شبكة
- خطوط monospace للتمييز
- تأثيرات نص glitch أو scramble
- لوحة سماوي، ماجنتا، أزرق كهربائي

### مرح / ودود
- سهولة زنبركية نطاطة (فيزياء الزنبرك)
- زوايا دائرية (نصف قطر كبير)
- ألوان باستيل أو زاهية
- حركات طافية/متأرجحة
- عناصر مرسومة يدوياً أو مصورة

### احترافي / مؤسسي
- حركات دقيقة، سريعة (200-300 مللي ثانية)
- خطوط sans-serif نظيفة
- خلفيات كحلي، رمادي، أو فحمي
- تباعد ومحاذاة دقيقة
- عناصر زخرفية قليلة
- تركيز على تصوير البيانات

### هادئ / بسيط
- حركة بطيئة جداً، دقيقة
- مسافات بيضاء عالية
- لوحة ألوان خافتة
- خطوط serif
- تباعد سخي
- تركيز على المحتوى، بدون تشتيت

### تحريري / مجلة
- تسلسل خطوط قوي
- اقتباسات مميزة ونداءات
- تفاعل صورة-نص
- تخطيطات كاسرة للشبكة
- عناوين serif، نص sans-serif
- أبيض وأسود مع لون تمييز واحد

---

## مرجع أنماط الحركة

### حركات الدخول

```css
/* ظهور + انزلاق للأعلى (الأكثر شيوعاً) */
.reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s var(--ease-out-expo),
                transform 0.6s var(--ease-out-expo);
}

.visible .reveal {
    opacity: 1;
    transform: translateY(0);
}

/* تكبير لداخل */
.reveal-scale {
    opacity: 0;
    transform: scale(0.9);
    transition: opacity 0.6s, transform 0.6s var(--ease-out-expo);
}

/* انزلاق من اليمين (للـ RTL) */
.reveal-right {
    opacity: 0;
    transform: translateX(-50px);
    transition: opacity 0.6s, transform 0.6s var(--ease-out-expo);
}

/* انزلاق من اليسار (للـ RTL) */
.reveal-left {
    opacity: 0;
    transform: translateX(50px);
    transition: opacity 0.6s, transform 0.6s var(--ease-out-expo);
}

/* ضبابية للداخل */
.reveal-blur {
    opacity: 0;
    filter: blur(10px);
    transition: opacity 0.8s, filter 0.8s var(--ease-out-expo);
}
```

### تأثيرات الخلفية

```css
/* شبكة تدرج */
.gradient-bg {
    background:
        radial-gradient(ellipse at 20% 80%, rgba(120, 0, 255, 0.3) 0%, transparent 50%),
        radial-gradient(ellipse at 80% 20%, rgba(0, 255, 200, 0.2) 0%, transparent 50%),
        var(--bg-primary);
}

/* نسيج تشويش */
.noise-bg {
    background-image: url("data:image/svg+xml,..."); /* تشويش SVG مضمّن */
}

/* نمط شبكة */
.grid-bg {
    background-image:
        linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
    background-size: 50px 50px;
}
```

### التأثيرات التفاعلية

```javascript
/* ميلان 3D عند التمرير */
class TiltEffect {
    constructor(element) {
        this.element = element;
        this.element.style.transformStyle = 'preserve-3d';
        this.element.style.perspective = '1000px';
        this.bindEvents();
    }

    bindEvents() {
        this.element.addEventListener('mousemove', (e) => {
            const rect = this.element.getBoundingClientRect();
            const x = (e.clientX - rect.left) / rect.width - 0.5;
            const y = (e.clientY - rect.top) / rect.height - 0.5;

            this.element.style.transform = `
                rotateY(${x * 10}deg)
                rotateX(${-y * 10}deg)
            `;
        });

        this.element.addEventListener('mouseleave', () => {
            this.element.style.transform = 'rotateY(0) rotateX(0)';
        });
    }
}
```

---

## استكشاف الأخطاء

### المشاكل الشائعة

**الخطوط لا تُحمّل:**
- تحقق من رابط Google Fonts
- تأكد من تطابق أسماء الخطوط في CSS

**الحركات لا تُفعّل:**
- تحقق من تشغيل Intersection Observer
- تحقق من إضافة صف `.visible`

**scroll snap لا يعمل:**
- تأكد من `scroll-snap-type` على html/body
- كل شريحة تحتاج `scroll-snap-align: start`

**مشاكل الجوال:**
- عطّل التأثيرات الثقيلة عند نقطة كسر 768px
- اختبر أحداث اللمس
- قلل عدد الجسيمات أو عطّل canvas

**مشاكل الأداء:**
- استخدم `will-change` باعتدال
- فضّل حركات `transform` و `opacity`
- اخنق معالجات scroll/mousemove

---

## المهارات ذات الصلة

- **learn** — إنشاء توثيق FORZARA.md للعرض
- **frontend-design** — لصفحات تفاعلية أكثر تعقيداً من الشرائح
- **design-and-refine:design-lab** — للتكرار على تصاميم المكونات

---

## مثال على سير الجلسة

1. المستخدم: "أريد إنشاء عرض تقديمي للمستثمرين لشركتي الناشئة في الذكاء الاصطناعي"
2. المهارة تسأل عن الغرض، الطول، المحتوى
3. المستخدم يشارك نقاطه ورسائله الرئيسية
4. المهارة تسأل عن الشعور المطلوب (معجب + متحمس)
5. المهارة تُنشئ 3 معاينات أسلوب
6. المستخدم يختار الأسلوب ب (سايبر نيون)، يطلب خلفية أغمق
7. المهارة تُنشئ العرض الكامل بجميع الشرائح
8. المهارة تفتح العرض في المتصفح
9. المستخدم يطلب تعديلات على شرائح محددة
10. تسليم العرض النهائي

---

## سير جلسة التحويل

1. المستخدم: "حوّل ملف slides.pptx إلى عرض ويب"
2. المهارة تستخرج المحتوى والصور من PPT
3. المهارة تؤكد المحتوى المستخرج مع المستخدم
4. المهارة تسأل عن الشعور/الأسلوب المطلوب
5. المهارة تُنشئ معاينات الأسلوب
6. المستخدم يختار أسلوباً
7. المهارة تُنشئ عرض HTML مع الأصول المحفوظة
8. تسليم العرض النهائي
